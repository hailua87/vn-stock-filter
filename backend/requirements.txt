# Core
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
pyarrow>=14.0.0          # parquet cache

# Data source
# KHONG con vnstock/vnai (26/09/2026). PyPI cach ly ca hai tu 24-25/09/2026 nen
# `pip install` hong va Daily Scan + CI do. Du lieu nay goi thang API Vietcap/KBS
# qua backend/scanner/sources/ (chi can `requests`). Ly do day du o docstring
# cua backend/scanner/sources/__init__.py.
#
# Neu sau nay muon dung lai vnstock: KHONG cai tu nguon ngoai PyPI de "chua
# chay" — ban truoc 4.0.9 ghi chi dan vao tep cau hinh tro ly AI moi lan import.
requests>=2.31.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Optional: testing & dev
pytest>=8.0.0
pytest-cov>=4.1.0
